import 'package:flutter/material.dart';

class ExpansibleDateBody extends StatelessWidget {
  const ExpansibleDateBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}