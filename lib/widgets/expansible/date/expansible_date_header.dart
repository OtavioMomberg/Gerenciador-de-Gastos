import 'package:flutter/material.dart';

class ExpansibleDateHeader extends StatelessWidget {
  const ExpansibleDateHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}